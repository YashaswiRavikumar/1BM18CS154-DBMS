CREATE TABLE BRANCH
(
    BRANCH_NAME VARCHAR2(20),
    BRANCH_CITY VARCHAR2(20),
    ASSETS NUMBER(10,2),
    PRIMARY KEY(BRANCH_NAME)
);

CREATE TABLE ACCOUNT
(
    ACCOUNT_NO NUMBER(10),
    BRANCH_NAME VARCHAR2(20),
    BALANCE NUMBER(10,2),
    FOREIGN KEY(BRANCH_NAME) REFERENCES BRANCH(BRANCH_NAME),
    PRIMARY KEY(ACCOUNT_NO)
);

CREATE TABLE DEPOSITOR
(
    CUSTOMER_NAME VARCHAR2(20),
    ACCOUNT_NO NUMBER(10),
    FOREIGN KEY(ACCOUNT_NO) REFERENCES ACCOUNT(ACCOUNT_NO),
    PRIMARY KEY(CUSTOMER_NAME)
);

CREATE TABLE CUSTOMER
(
    CUSTOMER_NAME VARCHAR2(20),
    CUSTOMER_STREET VARCHAR2(20),
    CITY VARCHAR2(20),
    FOREIGN KEY(CUSTOMER_NAME) REFERENCES DEPOSITOR(CUSTOMER_NAME)
);

CREATE TABLE LOAN
(
    LOAN_NUMBER NUMBER(20),
    BRANCH_NAME VARCHAR2(20),
    AMOUNT NUMBER(20),
    FOREIGN KEY(BRANCH_NAME) REFERENCES BRANCH(BRANCH_NAME),
    PRIMARY KEY(LOAN_NUMBER)
);

CREATE TABLE BORROWER
(
    CUSTOMER_NAME VARCHAR2(20),
    LOAN_NUMBER NUMBER(20),
    FOREIGN KEY(CUSTOMER_NAME) REFERENCES DEPOSITOR(CUSTOMER_NAME),
    FOREIGN KEY(LOAN_NUMBER) REFERENCES LOAN(LOAN_NUMBER)
);
  





INSERT INTO BRANCH VALUES('&BRANCH_NAME','&BRANCH_CITY',&ASSETS);

INSERT INTO ACCOUNT VALUES(&ACCOUNT_NO,'&BRANCH_NAME',&BALANCE);

INSERT INTO DEPOSITOR VALUES('&CUSTOMER_NAME',&ACCOUNT_NO);

INSERT INTO CUSTOMER VALUES('&CUSTOMER_NAME','&CUSTOMER_STREET','&CITY');

INSERT INTO LOAN VALUES(&LOAN_NUMBER,'&BRANCH_NAME',&AMOUNT);

INSERT INTO BORROWER VALUES('&CUSTOMER_NAME',&LOAN_NUMBER);

SELECT * FROM BRANCH;

SELECT * FROM ACCOUNT;

SELECT * FROM DEPOSITOR;

SELECT * FROM CUSTOMER;

SELECT * FROM LOAN;

SELECT * FROM BORROWER;  